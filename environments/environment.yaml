name: healdit
channels:
  - conda-forge
  - defaults
dependencies:
  # Core top-level packages
  - xesmf=0.9.2
  - xarray
  - cf_xarray
  - pandas
  - numpy
  - numba
  - shapely
  - netcdf-fortran
  - esmpy
  # Standard Python environment
  - python=3.12
  - pip
  - wheel
  - setuptools